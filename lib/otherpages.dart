import 'package:flutter/material.dart';

class EventMapPage extends StatelessWidget {
  const EventMapPage({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(
    // appBar: AppBar(title: const Text('Event Map')),
    body: const Center(child: Text('Event Map content goes here')),
  );
}

class AutoLeaderboardPage extends StatelessWidget {
  const AutoLeaderboardPage({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(
    // appBar: AppBar(title: const Text('Autonomous Leaderboard')),
    body: const Center(child: Text('Autonomous Leaderboard content goes here')),
  );
}
